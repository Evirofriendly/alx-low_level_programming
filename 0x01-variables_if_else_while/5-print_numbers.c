#include <stdio.h>
#include <ctype.h>

/**
 * main - Entry program point printing numbers
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int y;

	for (y = 0 ; y < 10 ; y++)
	{
		printf("%d", y);
		printf("\n");
	}
	return (0);
}
