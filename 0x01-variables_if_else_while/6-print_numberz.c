#include <stdio.h>

/**
 * main - printing alphabet with character
 *
 * Return: Always 0
 */

int main(void)
{
	int f;

	for (f = 0; f < 10; f++)
	{
		putchar('0' + f);
	}
	putchar('\n');
	return (0);
}
