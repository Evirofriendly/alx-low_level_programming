#include "main.h"

/**
 * _abs - compute absolute value of an integer
 * @n: check for the integer
 *
 * Return: For absolute integer
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
